Serialize RDF in a different format and wrap in VIVO Sparql Update syntax

```
$ ./rdf-serial.rb /path/to/file.rdf ntriples
```

```
$ jsonld-serial.rb /path/to/file.json ntriples
```

Other output serializations can be:

ttl
ntriples
nquads
rdfxml
